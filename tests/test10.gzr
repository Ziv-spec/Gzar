
a: string = "this works my friend";

puts :: (val: string) -> int; 

square :: (x: int) -> int {
	return x*x;
}

main :: () -> int {
	x: int = 2; 
	
	x_squared: int = square(x); 

	puts(a); 

	return x_squared;
}